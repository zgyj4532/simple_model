# AGENTS.md

(See `docs/AGENT_QUICKSTART.md` for the 5-minute orientation; this file is being
finalized by another agent and will replace this placeholder shortly.)

The bash+jq runtime is the algorithm; LLMs are leaf workers.
